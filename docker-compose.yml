version: "3.9"

services:
  web:
    build: .
    container_name: lukomorie_web
    env_file:
      - .env
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8001 --workers 3
    ports:
      - "8081:8001"
    volumes:
      - .:/app
    restart: always