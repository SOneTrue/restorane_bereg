services:
  web:
    build: .
    container_name: bereg_web
    ports:
      - "8081:8001"
    volumes:
      - .:/app
    restart: always
    command: python -m gunicorn maksimenko.wsgi:application --bind 0.0.0.0:8001 --workers 3